# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
.tox/
.venv/
venv/
env/
.ridge/
.env
.env.local
.secrets/

# Painter generated data (non-reproducible, large, or sensitive)
journal.jsonl
journal.jsonl.bak_*
runs/*
!runs/.gitkeep
reflections/*
!reflections/.gitkeep
# Internal benchmark history — not part of the public hackathon artifact.
# Previously we kept per-run bench.json summaries tracked; now the whole
# directory is local-only.
benches/
targets/unsplash/
targets/*.jpg
targets/*.jpeg
targets/*.png
*.log
*.tmp
painter_canvas.png
painter_target.png
painter_heatmap.png
/tmp/painter_*.png
/tmp/*.png

# IDE / OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# Editor/agent local state
.claude/
.hermes/
*.sublime-workspace
*.sublime-project

# Build / dist
dist/
build/
*.egg

# Data science / notebooks (if added later)
*.ipynb
.ipynb_checkpoints/

# OS-specific
nohup.out
*.pid

# Internal process artifacts — kept local only, not part of the public submission
CODE_REVIEW.md
docs/hackathon-judge-review-and-plan.md
docs/sessions/
docs/submission-copy.md
docs/superpowers/plans/
docs/superpowers/specs/

# Local scratch / WIP — never commit these to the public tree
.playwright-mcp/
_wireZoom
viewer-revamp-full.png
